export type Supplier = {
  id: string;
  name: string;
  rut?: string;
  address?: string;
  phone?: string;
  email?: string;
  contactName?: string;
  notes?: string;
  createdAt: string;
  updatedAt: string;
};
